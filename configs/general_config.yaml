general:
  data:
    path: './data/merged_data.parquet'
    excluding_columns: ['series_id', 'date', 'step', 'night', 'event']
  tensorboard:
    path: './tensorboard'
  test_data:
    path: './data/merged_data.parquet'