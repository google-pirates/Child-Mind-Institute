general:
  data:
    path: './data/my_train.parquet'
    excluding_columns: ['series_id', 'date', 'step', 'night', 'event']
  tensorboard:
    path: './tensorboard'
  test_data:
    path: './data/my_train.parquet'
  log_dir: './tensorboard/acc,step=2,window_size=120,batch_size=256,lr=0.001,RADam/version_0/'